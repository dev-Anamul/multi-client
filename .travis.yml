sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t anamul/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true anamul/react-test npm test

after_success:
  - docker build -t anamul/multi-client ./client
  - docker build -t anamul/multi-nginx ./nginx
  - docker build -t anamul/multi-server ./server
  - docker build -t anamul/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push anamul/multi-client
  - docker push anamul/multi-nginx
  - docker push anamul/multi-server
  - docker push anamul/multi-worker
